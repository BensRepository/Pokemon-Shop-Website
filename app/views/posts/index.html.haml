%h2= t('.title')
%h4= t('.description')
%title= t('.title')
= form_for "",url: posts_path, role: "search", method: :get do
  = text_field_tag :search, @search_term,placeholder: "Search..."

%table{:cellpadding => "0", :cellspacing => "0"}
#striped2
  .striped
    %table{:style => "width:100%"}

      %thead
        %tr
          %th= t('.heading1')
          %th= t('.heading2')
          %th= t('.heading3')
          %th
          %th
          %th

      %tbody
        - @posts.each do |post|
          %tr
            %td= post.title
            %td= post.author
            %td= post.date
            %td= link_to t('read'), post
            -if post.user == current_user
              %td= link_to  t('edit'), edit_post_path(post)
              %td= link_to t('delete'), post, method: :delete, data: { confirm: 'Are you sure?' }


    %br

    %h3= link_to t('.new'), new_post_path
